<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- 🎨 ألوان -->
    <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#f0f2f5"/>
    <SolidColorBrush x:Key="PrimaryColorBrush" Color="#4A90E2"/>
    <SolidColorBrush x:Key="ButtonColor" Color="#32363E"/>
    <SolidColorBrush x:Key="ButtonHover" Color="#4A5059"/>
    <SolidColorBrush x:Key="ButtonPressed" Color="#292C32"/>
    <SolidColorBrush x:Key="AccentGreen" Color="#4caf50"/>

    <!-- 🔘 زر موحد -->
    <Style TargetType="Button" x:Key="DefaultButtonStyle">
        <Setter Property="Background" Value="{StaticResource ButtonColor}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource ButtonColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="Padding" Value="5"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="5">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource ButtonHover}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource ButtonPressed}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- 📝 تنسيقات النصوص -->
    <Style x:Key="CenterTextStyle" TargetType="TextBlock">
        <Setter Property="TextAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="RightTextStyle" TargetType="TextBlock">
        <Setter Property="TextAlignment" Value="Right"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- 📊 جدول موحد -->
    <Style TargetType="DataGrid" x:Key="DefaultDataGridStyle">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="RowBackground" Value="White"/>
        <Setter Property="AlternatingRowBackground" Value="#f7f7f7"/>
        <Setter Property="GridLinesVisibility" Value="Vertical"/>
    </Style>

</ResourceDictionary>
