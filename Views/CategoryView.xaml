<UserControl x:Class="ETAG_ERP.Views.CategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="#F8F8F8">

    <UserControl.Resources>
        <!-- 🟢 ستايل الأزرار -->
        <Style TargetType="Button" x:Key="GreenButtonStyle" BasedOn="{StaticResource CardButtonStyle}">
            <Setter Property="Background" Value="#2C3E50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#2C3E50"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <!-- TextBox Placeholder -->
        <Style TargetType="TextBox" x:Key="PlaceholderTextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <ScrollViewer x:Name="PART_ContentHost" />
                            <TextBlock Text="ابحث في التصنيفات"
                                       Foreground="Gray"
                                       Margin="5,1,0,-1"
                                       IsHitTestVisible="False"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 🟦 كارت الصنف مع Animation -->
        <DataTemplate x:Key="ProductCardTemplate">
            <Border CornerRadius="12" BorderBrush="#DDD" BorderThickness="1"
                    Padding="10" Margin="10"
                    Background="White" Width="160" Height="190">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.25"/>
                </Border.Effect>
                <Border.RenderTransform>
                    <ScaleTransform x:Name="cardScale" ScaleX="1" ScaleY="1"/>
                </Border.RenderTransform>
                <Border.Triggers>
                    <EventTrigger RoutedEvent="MouseEnter">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetName="cardScale" Storyboard.TargetProperty="ScaleX"
                                                 To="1.05" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetName="cardScale" Storyboard.TargetProperty="ScaleY"
                                                 To="1.05" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                    <EventTrigger RoutedEvent="MouseLeave">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetName="cardScale" Storyboard.TargetProperty="ScaleX"
                                                 To="1" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetName="cardScale" Storyboard.TargetProperty="ScaleY"
                                                 To="1" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Border.Triggers>

                <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                    <Image Source="{Binding ImagePath}" Width="80" Height="80" Margin="0,0,0,10"
                           Stretch="UniformToFill"/>
                    <TextBlock Text="{Binding ItemName}" TextAlignment="Center"
                               FontWeight="SemiBold" FontSize="14"
                               Foreground="#2C3E50" TextWrapping="Wrap"/>
                    <Button Content="★" Width="30" Height="30"
                            HorizontalAlignment="Center"
                            ToolTip="إضافة للمفضلة"
                            Style="{StaticResource GreenButtonStyle}"
                            FontSize="14" Padding="0"/>
                </StackPanel>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="20" Background="#F8F8F8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 🏷️ الهيدر -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15" VerticalAlignment="Center">
            <TextBlock Text="📂 إدارة التصنيفات" FontSize="22" FontWeight="Bold" Foreground="#2C3E50"/>
            <TextBlock x:Name="CategoryTitle" Text="(الكل)" FontSize="16"
                       Foreground="Gray" Margin="15,5,0,0"/>
            <Button
            Content="➕ الاصناف"
            Width="200" Height="40"
            HorizontalAlignment="Left"
            Click="Show_Edit_Item_Click"
            Style="{StaticResource GreenButtonStyle}" Margin="157,2,0,2"/>
        </StackPanel>

        <!-- مربع البحث -->
        <TextBox x:Name="ProductSearchTextBox"
                 Style="{StaticResource PlaceholderTextBox}"
                 FontSize="16"
                 Margin="0,9,225,12"
                 VerticalContentAlignment="Center"
                 TextChanged="ProductSearchTextBox_TextChanged" Grid.Row="1"/>

        <!-- زر إضافة صنف جديد -->
        <Button Grid.Row="1"
                Content="➕ إضافة صنف جديد"
                Width="200" Height="40"
                HorizontalAlignment="Right"
                Click="AddItem_Click"
                Style="{StaticResource GreenButtonStyle}"/>

        <!-- 🟢 كروت التصنيفات -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="CategoryCardsPanel"
                          ItemTemplate="{StaticResource ProductCardTemplate}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel HorizontalAlignment="Center" Margin="10"/>

                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>

        <!-- أزرار التحكم -->
        <WrapPanel Grid.Row="3" HorizontalAlignment="Center" Margin="0,6,0,14">
            <Button Content="⬅ رجوع" Style="{StaticResource GreenButtonStyle}"
                    Click="BackCategory_Click" Margin="10" Height="46" Width="110"/>
            <Button Content="التالي ➡" Style="{StaticResource GreenButtonStyle}"
                    Click="NextCategory_Click" Margin="10" Height="44" Width="114"/>
        </WrapPanel>

    </Grid>
</UserControl>
