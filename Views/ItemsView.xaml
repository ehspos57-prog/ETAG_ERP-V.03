<UserControl x:Class="ETAG_ERP.Views.ItemsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- العنوان -->
        <TextBlock Text="إدارة الأصناف" FontSize="24" FontWeight="Bold" Foreground="DarkSlateGray" />

        <!-- جدول الأصناف -->
        <DataGrid x:Name="ItemsGrid" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,10,0,10" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="كود" Binding="{Binding Code}" Width="*" />
                <DataGridTextColumn Header="الاسم" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="الكمية" Binding="{Binding Quantity}" Width="*" />
                <DataGridTextColumn Header="سعر الشراء" Binding="{Binding PurchasePrice}" Width="*" />
                <DataGridTextColumn Header="سعر البيع" Binding="{Binding SellingPrice}" Width="*" />
                <DataGridTextColumn Header="سعر 1" Binding="{Binding Price1}" Width="*" />
                <DataGridTextColumn Header="سعر 2" Binding="{Binding Price2}" Width="*" />
                <DataGridTextColumn Header="سعر 3" Binding="{Binding Price3}" Width="*" />
                <DataGridTextColumn Header="عائلة رئيسية" Binding="{Binding CategoryLevel1}" Width="*" />
                <DataGridTextColumn Header="فرعية 2" Binding="{Binding CategoryLevel2}" Width="*" />
                <DataGridTextColumn Header="فرعية 3" Binding="{Binding CategoryLevel3}" Width="*" />
                <DataGridTextColumn Header="فرعية 4" Binding="{Binding CategoryLevel4}" Width="*" />
                <DataGridTextColumn Header="فرعية 5" Binding="{Binding CategoryLevel5}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- أزرار التحكم -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="➕ إضافة" Click="AddItem_Click" Margin="5"/>
            <Button Content="✏️ تعديل" Click="EditItem_Click" Margin="5"/>
            <Button Content="🗑️ حذف" Click="DeleteItem_Click" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>
