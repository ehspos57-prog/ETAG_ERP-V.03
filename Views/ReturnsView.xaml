<UserControl x:Class="ETAG_ERP.Views.ReturnsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="1100"
             FlowDirection="RightToLeft"
             Background="#F8F8F8">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 🔍 فلاتر البحث -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0 0 0 10">
            <TextBlock Text="من:" Margin="5" VerticalAlignment="Center"/>
            <DatePicker Width="120" SelectedDate="{Binding FromDate}" Margin="0,0,10,0"/>

            <TextBlock Text="إلى:" Margin="5" VerticalAlignment="Center"/>
            <DatePicker Width="120" SelectedDate="{Binding ToDate}" Margin="0,0,10,0"/>

            <TextBlock Text="العميل:" Margin="5" VerticalAlignment="Center"/>
            <ComboBox Width="150" ItemsSource="{Binding Clients}" SelectedItem="{Binding SelectedClient}" Margin="0,0,10,0"/>

            <TextBox Width="200" Margin="0,0,10,0" Text="{Binding SearchText}" ToolTip="ابحث عن المرتجع أو كلمة مفتاحية"/>
            <Button Content="تصفية" Width="100" Command="{Binding FilterCommand}" />
        </StackPanel>

        <!-- 📋 جدول المرتجعات -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding ReturnList}"
                  SelectedItem="{Binding SelectedReturn}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  Margin="0 0 0 10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="رقم المرتجع" Binding="{Binding InvoiceNumber}" Width="*"/>
                <DataGridTextColumn Header="التاريخ" Binding="{Binding Date}" Width="*"/>
                <DataGridTextColumn Header="العميل" Binding="{Binding ClientName}" Width="*"/>
                <DataGridTextColumn Header="الإجمالي" Binding="{Binding TotalAmount}" Width="*"/>
                <DataGridTextColumn Header="المستخدم" Binding="{Binding Username}" Width="*"/>
                <DataGridTextColumn Header="الوصف" Binding="{Binding Description}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- ✅ أزرار العمليات -->
        <DockPanel Grid.Row="2" Margin="0,5">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <Button Content="إضافة مرتجع" Width="120" Margin="5" Command="{Binding AddReturnCommand}" />
                <Button Content="تعديل" Width="80" Margin="5" Command="{Binding EditReturnCommand}" />
                <Button Content="حذف" Width="80" Margin="5" Command="{Binding DeleteReturnCommand}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Content="تحديث" Width="80" Margin="5" Command="{Binding RefreshCommand}" />
                <Button Content="تصدير" Width="100" Margin="5" Command="{Binding ExportCommand}" />
                <Button Content="طباعة" Width="80" Margin="5" Command="{Binding PrintCommand}" />
                <Button Content="تفاصيل" Width="100" Margin="5" Command="{Binding OpenDetailsCommand}" />
            </StackPanel>
        </DockPanel>
    </Grid>
</UserControl>
