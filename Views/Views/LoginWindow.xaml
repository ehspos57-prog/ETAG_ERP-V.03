<Window x:Class="ETAG_POS.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="تسجيل الدخول" Height="450" Width="350" ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        FlowDirection="RightToLeft" FontFamily="Segoe UI" Background="#f0f0f0">

    <Window.Resources>
        <!-- ستايل زر المستخدم -->
        <Style x:Key="UserButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#ccc"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,2"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#e0f0ff"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#4a90e2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#a7c7ff"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#2a6bd1"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#cce4ff"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#1d4f91"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#ddd"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#aaa"/>
                                <Setter Property="Foreground" Value="#888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- العنوان -->
        <TextBlock Text="اختر المستخدم ثم أدخل كلمة المرور" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <!-- قائمة المستخدمين -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Height="220" Margin="0,0,0,10">
            <ItemsControl x:Name="UsersList" />
        </ScrollViewer>

        <!-- المستخدم المختار -->
        <TextBlock x:Name="txtSelectedUser" Grid.Row="2" Text="لم يتم اختيار مستخدم" FontWeight="SemiBold" FontSize="16" Margin="0,5,0,15" Foreground="#333"/>

        <!-- حقل كلمة المرور -->
        <PasswordBox x:Name="txtPassword" Grid.Row="3" Height="35" FontSize="16" Padding="5" />

        <!-- أزرار تسجيل الدخول والخروج -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0" >

            <Button Content="تسجيل الدخول" Width="130" Height="40" Margin="10,0" Background="#4a90e2" Foreground="White" FontWeight="Bold" Click="Login_Click" />

            <Button Content="خروج" Width="130" Height="40" Margin="10,0" Background="#d9534f" Foreground="White" FontWeight="Bold" Click="Exit_Click" />

        </StackPanel>

    </Grid>
</Window>
