<Window x:Class="ETAG_ERP.Views.ReceivingNoteView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="إذن صرف" Height="720" Width="1100"
        WindowStartupLocation="CenterScreen"
        FlowDirection="RightToLeft"
        Background="#FAFAFA">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- بيانات إذن الاستلام -->
        <StackPanel Orientation="Horizontal" Margin="0 0 0 10" Grid.Row="0">
            <TextBlock Text="المورد:" Margin="5" VerticalAlignment="Center"/>
            <TextBox Width="200" Text="{Binding SupplierName}" Margin="0,0,10,0"/>

            <TextBlock Text="رقم المرجع:" Margin="5" VerticalAlignment="Center"/>
            <TextBox Width="150" Text="{Binding ReferenceNumber}" Margin="0,0,10,0"/>

            <TextBlock Text="التاريخ:" Margin="5" VerticalAlignment="Center"/>
            <DatePicker Width="120" SelectedDate="{Binding NoteDate}" Margin="0,0,10,0"/>
        </StackPanel>

        <!-- جدول الأصناف -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Items}" AutoGenerateColumns="False"
                  CanUserAddRows="False" Margin="0 0 0 10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="اسم الصنف" Binding="{Binding ItemName}" Width="2*"/>
                <DataGridTextColumn Header="الكود" Binding="{Binding ItemCode}" Width="*"/>
                <DataGridTextColumn Header="الكمية المستلمة" Binding="{Binding Quantity}" Width="*"/>
                <DataGridTextColumn Header="ملاحظات" Binding="{Binding Remarks}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- أزرار العمليات -->
        <DockPanel Grid.Row="2">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <Button Content="إضافة صنف" Width="100" Margin="5" Command="{Binding AddItemCommand}"/>
                <Button Content="حذف صنف" Width="100" Margin="5" Command="{Binding RemoveItemCommand}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Content="حفظ" Width="100" Margin="5" Command="{Binding SaveCommand}"/>
                <Button Content="طباعة" Width="100" Margin="5" Command="{Binding PrintCommand}"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
