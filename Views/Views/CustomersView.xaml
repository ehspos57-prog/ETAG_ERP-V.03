<UserControl x:Class="ETAG_ERP.Views.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- العنوان -->
            <RowDefinition Height="Auto"/>
            <!-- البحث -->
            <RowDefinition Height="Auto"/>
            <!-- الأزرار -->
            <RowDefinition Height="*"/>
            <!-- الجدول -->
            <RowDefinition Height="Auto"/>
            <!-- الملخص -->
        </Grid.RowDefinitions>

        <!-- العنوان -->
        <TextBlock Text="📋 إدارة العملاء" 
                   FontSize="26" FontWeight="Bold" 
                   Foreground="#FF3F79D8" 
                   Margin="0,0,0,15" Grid.Row="0"/>

        <!-- البحث -->
        <DockPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBox x:Name="SearchBox" Width="200" Height="28" 
                     Margin="5" VerticalContentAlignment="Center"/>
            <Button Content="بحث" Width="80" Height="28" 
                    Background="#FF3F79D8" Foreground="White" Margin="5"
                    Click="Search_Click"/>
        </DockPanel>

        <!-- الأزرار -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10" Grid.Row="2">
            <Button Content="➕ إضافة" Width="110" Height="32" Margin="5" Click="AddClient_Click"/>
            <Button Content="✏️ تعديل" Width="110" Height="32" Margin="5" Click="EditClient_Click"/>
            <Button Content="🗑️ حذف" Width="110" Height="32" Margin="5" Click="DeleteClient_Click"/>
            <Button Content="💾 حفظ" Width="100" Height="32" Margin="5" Click="SaveClients_Click"/>
            <Button Content="↩️ تراجع" Width="100" Height="32" Margin="5" Click="CancelChanges_Click"/>
        </StackPanel>

        <!-- جدول العملاء -->
        <DataGrid x:Name="ClientsGrid" Grid.Row="3" 
                  AutoGenerateColumns="False" CanUserAddRows="False" 
                  HeadersVisibility="Column"
                  RowHeight="30"
                  AlternatingRowBackground="#f9f9f9"
                  GridLinesVisibility="None"
                  BorderBrush="DarkGreen" BorderThickness="1"
                  ItemsSource="{Binding ClientsList}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="الاسم" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="رقم الهاتف" Binding="{Binding Phone}" Width="150"/>
                <DataGridTextColumn Header="العنوان" Binding="{Binding Address}" Width="220"/>
                <DataGridTextColumn Header="الرصيد" Binding="{Binding Balance}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- الملخص -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,10,0,0">
            <TextBlock Text="إجمالي العملاء: " FontWeight="Bold" Foreground="#FF3F79D8"/>
            <TextBlock Text="{Binding ClientsList.Count}" Margin="5,0"/>
            <TextBlock Text=" | إجمالي الأرصدة: " FontWeight="Bold" Foreground="#FF3F79D8"/>
            <TextBlock Text="{Binding TotalBalance}" Margin="5,0"/>
        </StackPanel>
    </Grid>
</UserControl>
