<UserControl x:Class="ETAG_ERP.Views.PurchaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="1100" d:DesignHeight="720"
             FlowDirection="RightToLeft"
             Background="#F9F9F9">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- فلاتر -->
            <RowDefinition Height="*"/>
            <!-- جدول -->
            <RowDefinition Height="Auto"/>
            <!-- أزرار -->
            <RowDefinition Height="Auto"/>
            <!-- نموذج الإدخال -->
        </Grid.RowDefinitions>

        <!-- 🔍 الفلاتر -->
        <StackPanel Orientation="Horizontal" Margin="0 0 0 10" Grid.Row="0">
            <TextBlock Text="من:" Margin="5" VerticalAlignment="Center"/>
            <DatePicker x:Name="FromDatePicker" Width="120" Margin="0 0 10 0"/>

            <TextBlock Text="إلى:" Margin="5" VerticalAlignment="Center"/>
            <DatePicker x:Name="ToDatePicker" Width="120" Margin="0 0 10 0"/>

            <TextBlock Text="المورد:" Margin="5" VerticalAlignment="Center"/>
            <ComboBox x:Name="SupplierFilterComboBox" Width="150" Margin="0 0 10 0"/>

            <TextBox x:Name="SearchTextBox" Width="200" Margin="0 0 10 0" ToolTip="ابحث برقم الفاتورة أو وصف"/>

            <Button Content="تصفية" Width="100" Click="FilterButton_Click"/>
        </StackPanel>

        <!-- 📋 جدول الفواتير -->
        <DataGrid x:Name="PurchasesGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  Margin="0 0 0 10"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="رقم الفاتورة" Binding="{Binding InvoiceNumber}" Width="*"/>
                <DataGridTextColumn Header="التاريخ" Binding="{Binding Date, StringFormat=yyyy/MM/dd}" Width="*"/>
                <DataGridTextColumn Header="المورد" Binding="{Binding SupplierName}" Width="*"/>
                <DataGridTextColumn Header="الإجمالي" Binding="{Binding TotalAmount}" Width="*"/>
                <DataGridTextColumn Header="المستخدم" Binding="{Binding Username}" Width="*"/>
                <DataGridTextColumn Header="الوصف" Binding="{Binding Description}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- ✅ أزرار العمليات -->
        <DockPanel Grid.Row="2" Margin="0,5">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <Button x:Name="AddButton" Content="إضافة" Width="100" Margin="5" Click="AddButton_Click"/>
                <Button Content="تعديل" Width="80" Margin="5" IsEnabled="False"/>
                <Button Content="حذف" Width="80" Margin="5" IsEnabled="False"/>
                <Button Content="تفاصيل" Width="100" Margin="5" IsEnabled="False"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Content="تحديث" Width="80" Margin="5" Click="RefreshButton_Click"/>
                <Button Content="تصدير" Width="100" Margin="5" IsEnabled="False"/>
                <Button Content="طباعة" Width="80" Margin="5" IsEnabled="False"/>
            </StackPanel>
        </DockPanel>

        <!-- 🧾 نموذج الإدخال -->
        <Border x:Name="FormPanel"
                Grid.Row="3"
                BorderBrush="LightGray"
                BorderThickness="1"
                Padding="10"
                Margin="0 10 0 0"
                Background="#FFFFFF"
                Visibility="Collapsed">

            <StackPanel>
                <TextBlock Text="فاتورة مشتريات جديدة" FontWeight="Bold" FontSize="14" Margin="0 0 10 0"/>

                <StackPanel Orientation="Horizontal" Margin="0 0 10 10">
                    <TextBlock Text="المورد:" Margin="5" VerticalAlignment="Center"/>
                    <ComboBox x:Name="SupplierComboBox" Width="200" Margin="0 10 20 0"/>
                    <TextBlock Text="التاريخ:" Margin="5" VerticalAlignment="Center"/>
                    <DatePicker x:Name="PurchaseDatePicker" Width="150"/>
                </StackPanel>

                <TextBlock Text="الوصف:" Margin="5"/>
                <TextBox x:Name="DescriptionBox" Height="60" TextWrapping="Wrap"/>

                <DataGrid x:Name="ItemsGrid" AutoGenerateColumns="False" CanUserAddRows="True" Margin="0 10" Height="200">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="الصنف" Binding="{Binding ItemName}" Width="2*"/>
                        <DataGridTextColumn Header="الكمية" Binding="{Binding Quantity}" Width="*"/>
                        <DataGridTextColumn Header="السعر" Binding="{Binding UnitPrice}" Width="*"/>
                        <DataGridTextColumn Header="الإجمالي" Binding="{Binding Total}" Width="*" IsReadOnly="True"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 10">
                    <TextBlock Text="الإجمالي الكلي:" VerticalAlignment="Center" Margin="5"/>
                    <TextBlock x:Name="TotalAmountText" FontWeight="Bold" VerticalAlignment="Center" Margin="5"/>
                    <Button Content="حفظ" Width="100" Margin="10" Click="SaveButton_Click"/>
                    <Button Content="إلغاء" Width="100" Margin="10" Click="CancelButton_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
