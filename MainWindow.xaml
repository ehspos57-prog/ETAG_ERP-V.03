<Window x:Class="ETAG_ERP.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ETAG_ERP.Views"
        Title="ETAG ERP - الشاشة الرئيسية"
        WindowStartupLocation="CenterScreen"
        WindowStyle="SingleBorderWindow"
        mc:Ignorable="d"
        Height="800" Width="1300" ResizeMode="CanResizeWithGrip">



    <Window.Resources>
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="10 5"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF394867"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ✅ القائمة الجانبية مع Scroll -->
        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
            <StackPanel Grid.Column="0" Background="#2C3E50" Orientation="Vertical">
                <TextBlock Text="ETAG ERP" Foreground="White" FontSize="24" FontWeight="Bold"
                       Margin="10" HorizontalAlignment="Center"/>

                <Separator Margin="10"/>


                <Button Style="{StaticResource SidebarButtonStyle}" Content="📦 الأصناف" Click="الأصناف_Click" Height="35"  Margin="3"/>
                <Button Style="{StaticResource SidebarButtonStyle}" Content="👥 العملاء" Click="العملاء_Click" Height="35"  Margin="3"/>
                <Button Style="{StaticResource SidebarButtonStyle}" Content="🧾 الفواتير" Click="فاتورة_Click" Height="35"  Margin="3"/>
                <Button Style="{StaticResource SidebarButtonStyle}" Content="📊 الحسابات" Click="الحسابات_Click" Height="35"  Margin="3"/>
                <Button Style="{StaticResource SidebarButtonStyle}" Content="🎯 عروض الأسعار" Click="عروض_الأسعار_Click" Height="35"  Margin="3"/>
                <Button Style="{StaticResource SidebarButtonStyle}" Content="🆕 تقارير" Click="تقارير_Click" Height="35"  Margin="5"/>
                <Button Style="{StaticResource SidebarButtonStyle}" Content="🆕 إذن صرف" Click="فاتورة_جديدة_Click" Height="35"  Margin="3"/>
                <Button Style="{StaticResource SidebarButtonStyle}" Content="📥 البوابة" Click="البوابة_Click" Height="35" Margin="3"/>
                <Button Style="{StaticResource SidebarButtonStyle}" Content="📥 مصروفات" Click="Expenses_Click" Height="35"  Margin="3"/>

                <Button Style="{StaticResource SidebarButtonStyle}" Content="♻️ المرتجعات" Click="المرتجعات_Click" Height="35"  Margin="3"/>
                <Button Style="{StaticResource SidebarButtonStyle}" Content="📨 فاتورة ضريبية" Click="إذن_الاستلام_Click" Height="35"  Margin="3"/>
                <Button Style="{StaticResource SidebarButtonStyle}" Content="📄 مندوبين" Click="مندوبين_Click" Height="35"  Margin="3"/>
                <Button Style="{StaticResource SidebarButtonStyle}" Content="📋 جرد المخزون" Click="جرد_المخزون_Click" Height="35"  Margin="3"/>
                <Button Style="{StaticResource SidebarButtonStyle}" Content="📈 كشف حساب" Click="كشف_حساب_Click" Height="35"  Margin="3"/>
                <Button Style="{StaticResource SidebarButtonStyle}" Content="⚙️ الإعدادات" Click="الإعدادات_Click" Height="35"  Margin="5"/>
                <Button Style="{StaticResource SidebarButtonStyle}" Content="🚪 تسجيل خروج" Click="إغلاق_Click" Height="35"  Margin="3" />


            </StackPanel>
        </ScrollViewer>



        <!-- ===== الواجهة الرئيسية (العرض الديناميكي) ===== -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- شريط علوي -->
                <RowDefinition Height="*"/>
                <!-- المنطقة الديناميكية -->
                <RowDefinition Height="Auto"/>
                <!-- الشريط السفلي -->
            </Grid.RowDefinitions>

            <!-- الشريط العلوي -->
            <DockPanel Grid.Row="0" Background="#34495E" Margin="0,0,0,720" Grid.RowSpan="2">
                <TextBlock Text="الرئيسية" Foreground="White" FontSize="18" Margin="10,0" DockPanel.Dock="Right" Width="59" Height="33"/>

            </DockPanel>
            <StackPanel VerticalAlignment="Bottom" Margin="10">
                <TextBlock Text="© E-tag For EHS" Foreground="White" FontSize="10" Height="19"/>
            </StackPanel>
            <TextBlock Text="مرحبًا، في البرنامج" Foreground="White" FontSize="14" Margin="10,32,10,688" Grid.RowSpan="2"/>

            <!-- المحتوى الديناميكي -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <ContentControl x:Name="ContentArea" RenderTransformOrigin="0.5,0.5">
                    <ContentControl.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform/>
                            <SkewTransform AngleX="0"/>
                            <RotateTransform/>
                            <TranslateTransform X="0"/>
                        </TransformGroup>
                    </ContentControl.RenderTransform>
                </ContentControl>

            </ScrollViewer>

        </Grid>
    </Grid>
</Window>
